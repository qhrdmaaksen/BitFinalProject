<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="MapProductreivews">

	<insert id="InsertData" parameterType="MyProductreviews">
	insert into members(mno, btitle, bwriter, bcontent, bimg, breview, bregdate)
	values (#{mno}, #{btitle}, #{bwriter}, #{bcontent}, #{bimg}, #{breview}, #{bregdate})
	</insert>
	
	<select id="SelectTotalCount" resultType="int">
		select count(*) as cnt from productreviews 
	<if test="mode == 'bwriter'">
		where bwriter like #{keyword}
	</if>	
	<if test="mode == 'bcontent'">
		where bcontent like #{keyword}
	</if>
	</select>
	<sql id="myselect">
		select bno, btitle, bwriter, bcontent, bimg, breview, bregdate
		from productreviews 
	</sql>
	<select id="SelectDataList" resultType="MyProductreviews">
		<include refid="myselect" />
		<if test="mode == 'bwriter'">
			where bwriter like #{keyword}
		</if>
		<if test="mode == 'btitle'">
			where btitle like #{keyword}
		</if>
		<if test="mode == 'bcontent'">
			where bcontent like #{keyword}
		</if>
		order by groupno desc, orderno asc 
	</select>

</mapper>